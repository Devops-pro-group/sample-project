1- Create a line of development from the main branch

  # create a branch and swith to that branch
        git checkout -b  feature1 

        git add .
        git commit -m ""
        git push origin <branch>






